import { ReactNode } from "react";

export interface IInsertButton {
  style?: React.CSSProperties;
  children: ReactNode;
}

export interface IInsertButtonUI {
  style?: React.CSSProperties;
  children: ReactNode;
}
