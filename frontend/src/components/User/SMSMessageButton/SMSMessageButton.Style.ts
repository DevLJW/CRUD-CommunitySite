import styled from "@emotion/styled";
import Stack from "@mui/material/Stack";
import Button from "@mui/material/Button";

export const SMSMessageButtonStack = styled(Stack)``;
export const SMSMessagebutton = styled(Button)``;
